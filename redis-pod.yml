apiVersion: v1
kind: pod
metadata:
   name: redi-pod
   labels:
      name: redis-pod
      app: demo-voting-app
      


spec:
   containers:
      name: redis
      image: redis 
      ports:
         containerport: 6379